This is the IDL version of the GUI to determined the onset time.
So what you need to change is :
1. the directory of data saved, and properly download and distribute the data
2. Change the path of folder “common” which including some function
3. include the common to the IDL environment path.
4. The code are used in SSWIDL. some function are used from SSWIDL package
